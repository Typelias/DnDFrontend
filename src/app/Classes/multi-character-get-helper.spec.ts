import { MultiCharacterGetHelper } from './multi-character-get-helper';

describe('MultiCharacterGetHelper', () => {
  it('should create an instance', () => {
    expect(new MultiCharacterGetHelper()).toBeTruthy();
  });
});
